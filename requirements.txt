# Core required dependencies for APGI Framework
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
h5py>=3.8.0
seaborn>=0.12.0
statsmodels>=0.14.0
psutil>=5.9.0
customtkinter>=5.2.0

# NOTE: On Linux, tkinter is part of the Python standard library but requires
# the system package python3-tk to be installed separately:
# sudo apt-get install python3-tk

# Development and testing
pytest>=7.4.0
coverage>=6.0
hypothesis>=6.70.0
pytest-timeout>=2.0

# Type checking
types-requests>=2.31.0
types-seaborn>=0.12.0

# Optional dependency groups available via:
# pip install -e .[gui]        # GUI applications
# pip install -e .[dev]        # Development tools
# pip install -e .[ml]         # Machine learning
# pip install -e .[neural]     # Neural data processing
# pip install -e .[web]        # Web applications
# pip install -e .[all]        # All optional dependencies