"""
APGI Framework GUI - Main Package

This package provides a graphical user interface for the APGI Framework,
allowing users to configure and run experiments with various parameters.
"""

__version__ = "0.1.0"

import sys
from pathlib import Path

# Add the project root to the Python path
sys.path.append(str(Path(__file__).parent.parent))
